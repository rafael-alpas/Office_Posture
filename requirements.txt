# Core ML / DL
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Object Detection (YOLOv5 dependencies)
# The code uses 'torch.hub.load("ultralytics/yolov5", ...)' and executes 'python yolov5/train.py'
# This requires the dependencies of the classic YOLOv5 repo, not the new 'ultralytics' package.
# We'll list the key requirements for YOLOv5 inference and utilities.
# The ultralytics>=8.0.0 line is removed as it refers to the new repo/package, which is not the primary way YOLOv5 is used here.
PyYAML>=6.0
scipy>=1.9.3 
tqdm>=4.64.0 

# Computer Vision
opencv-python>=4.8.0
Pillow>=9.5.0 # Required by PyTorch/YOLOv5/MediaPipe

# Human Pose Estimation & Classifier
mediapipe>=0.10.14
tensorflow>=2.15.0
protobuf<=4.25.3  # To avoid compatibility issues with some versions of MediaPipe


# Utilities
matplotlib>=3.7.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0 # For data splitting and StandardScaler/joblib
joblib>=1.3.2 # Explicitly imported and used for saving the scaler